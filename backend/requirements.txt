fastapi
uvicorn[standard]
sqlalchemy[asyncio]
aiosqlite
httpx
python-dotenv
pydantic-settings

# LLM providers (install only what you need)
google-genai     # for LLM_PROVIDER=gemini
openai           # for LLM_PROVIDER=openai
anthropic        # for LLM_PROVIDER=anthropic
# Ollama uses httpx (already included) — no extra package needed
